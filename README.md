# dclee131.github.io

Personal website for Dongchan Lee

## Local preview

Install the dependencies with `bundle install`, then start the local Jekyll server:

```bash
bundle exec jekyll serve --livereload
```

Open [http://localhost:4000](http://localhost:4000) in a browser. Stop the server with `Ctrl+C`.
